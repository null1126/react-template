module.exports = {
  // 单行长度,超过则自动换行
  printWidth: 80,
  // 缩进长度
  tabWidth: 2,
  // 使用空格代替tab缩进
  useTabs: false,
  // 句末使用分号
  semi: false,
  // 使用单引号
  singleQuote: true,
  // 配置换行符格式为auto
  endOfLine: 'auto',
  // 单参数箭头函数参数周围使用圆括号-eg: (x) => xavoid：省略括号
  arrowParens: 'avoid',
  // 在对象前后添加空格-eg: { foo: bar }
  bracketSpacing: true,
  // 在已被preitter格式化的文件顶部加上标注
  insertPragma: false,
  // 多属性html标签的‘>’折行放置
  jsxBracketSameLine: false,
  rangeStart: 0,
  // 无需顶部注释即可格式化
  requirePragma: false,
  // 多行时尽可能打印尾随逗号
  trailingComma: 'none',
  // 使用空格代替tab缩进
  useTabs: false
}
